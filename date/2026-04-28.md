```
flowchart TB
    subgraph early["🔴 早期启动 — IRQ禁用 / 单CPU / bootmem"]
        direction TB
        A1["set_task_stack_end_magic()"] -->
        A2["setup_arch() — 架构初始化"] -->
        A3["mm_init() — mem_init → slab → vmalloc"] -->
        A4["sched_init() — 调度器"] -->
        A5["rcu_init() — RCU"] -->
        A6["init_IRQ() — 中断控制器"] -->
        A7["time_init() — 系统时钟"]
    end

    subgraph mid["🟡 中期启动 — IRQ使能 / initcalls"]
        direction TB
        B1["console_init() — 早期控制台"] -->
        B2["lockdep / locking_selftest"] -->
        B3["vfs_caches_init() / security_init()"] -->
        B4["rest_init() — 创建 init & kthreadd"]
    end

    subgraph kernel_init_phase["🟢 内核init线程 — PID 1"]
        direction TB
        C1["wait kthreadd_done"] -->
        C2["smp_init() — 启动从CPU"] -->
        C3["do_basic_setup()"] -->
        C4["do_initcalls() — 8级初始化"] -->
        C5["open /dev/console → dup ×2"] -->
        C6["prepare_namespace() — 挂载根fs"]
    end

    subgraph userland["🔵 进入用户空间 — SYSTEM_RUNNING"]
        direction TB
        D1["free_initmem() — 释放__init段"] -->
        D2["mark_readonly() — 内核只读保护"] -->
        D3["system_state = SYSTEM_RUNNING"] -->
        D4["do_execve → /sbin/init"]
    end

    early -->|"local_irq_enable()"| mid
    mid -->|"kernel_thread(kernel_init)"| kernel_init_phase
    kernel_init_phase -->|"free_initmem()"| userland

    style early fill:#2d1b1b,stroke:#ff6b6b,color:#ffcccc
    style mid fill:#2d2b1b,stroke:#ffd93d,color:#fff5cc
    style kernel_init_phase fill:#1b2d1b,stroke:#6bcb77,color:#ccffcc
    style userland fill:#1b1b2d,stroke:#4d96ff,color:#cce0ff

```